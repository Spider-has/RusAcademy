import { AtomMut, Rec } from '@reatom/core';
import { WithReducers } from './withReducers';
export type RecordAtom<T extends Rec> = WithReducers<AtomMut<T>, {
    merge(state: T, slice: Partial<T>): T;
    omit(state: T, ...keys: Array<keyof T>): T;
    reset(state: T, ...keys: Array<keyof T>): T;
}>;
export declare const reatomRecord: <T extends Rec<any>>(initState: T, name?: string) => RecordAtom<T>;
//# sourceMappingURL=reatomRecord.d.ts.map